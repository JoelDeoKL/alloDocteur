{% extends 'admin_template.html.twig' %}

{% block title %} AlloDocteur - Dossiers {% endblock %}

{% block body %}

  <div class="main-panel">
  <div class="content-wrapper">
    <div class="row">
      <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
          <div class="card-body">
            <h4 class="card-title">Liste des dossiers médicaux</h4>
            <div class="table-responsive">
              <table class="table table-striped">
                <thead>
                <tr>
                  <th>Patient</th>
                  <th>Description</th>
                  <th>Date creation</th>
                  <th>Actions</th>
                </tr>
                </thead>
                <tbody>
                {% for dossier in dossiers %}
                  <tr>
                    <td>{{ dossier.patient }}</td>
                    <td>{{ dossier.description_dossier }}</td>
                    <td>{{ dossier.date_creation }}</td>
                    <td>
                      <a href="" class="btn btn-primary">Voir</a>
                      <a href="{{ path("editer_dossier", {id: dossier.id}) }}" class="btn btn-primary">Modifier</a>
                      <a href="" class="btn btn-primary">Supprimer</a>
                    </td>
                  </tr>
                {% endfor %}
                </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
{% endblock %}